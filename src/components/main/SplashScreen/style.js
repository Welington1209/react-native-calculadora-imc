import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
    minWidth: "100%",
    alignItems: "center",
  },
  text: {
    color: "#fafafa",
    marginVertical: 20,
    fontSize: 18,
  },
});

export default styles;
